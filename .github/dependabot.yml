# Keeps the SHA-pinned actions current; the `# vX.Y.Z` comments are
# updated with them. Commit messages stay Conventional Commits.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns: ["*"]
