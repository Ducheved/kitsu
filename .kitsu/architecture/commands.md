+++
title = "Typed commands"
level = "component"
parent = "desktop"
paths = ["app/src-tauri/src/*.rs"]
uses = ["cli"]
+++
The Tauri commands the webview may call, and the window. Each is a narrow,
typed operation over the kitsu crate.
