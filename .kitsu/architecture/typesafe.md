+++
title = "TypeSafe System One (Jev)"
level = "external"
technology = "HTTPS, POST /v1/systemone, Bearer key"
+++
Answers typed questions about a state with calibrated probabilities; no
text generation. Sees the data a judgment is about (for permission triage:
the shell command and the worktree path) and Kitsu's fixed questions.
Never sees the key's value in anything but the Authorization header.
